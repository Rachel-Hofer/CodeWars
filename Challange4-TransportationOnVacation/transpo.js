let days = 1;

function rentalCarCost(days) {
  let total = days * 40;

  if(days > 0 && days <= 2){
    total
    return total
  }
  if(days >= 3 && days <= 6){
    return total -= 20
  }
  if(days >= 7){
    return total -= 50
  }
}